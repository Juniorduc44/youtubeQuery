# Set USE_OPENAI to True to attempt using OpenAI, False to use local host directly
USE_OPENAI=True

# OpenAI settings (required if USE_OPENAI=True)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-3.5-turbo

# Local host settings
LOCAL_HOST=http://localhost:11434
LOCAL_MODEL=llama3:8b